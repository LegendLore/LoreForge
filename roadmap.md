{
  "note": "Runtime seed data currently lives in js/database.js so GitHub Pages works without a fetch request.",
  "planned": "Move this to data/seed.json after a loader and migration layer are added."
}